{
  "name": "merlin-server",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node test.mjs && npm --prefix .. run test",
    "build": "npm --prefix .. run build",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run test && npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
